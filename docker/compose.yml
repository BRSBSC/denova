services:
  denova:
    image: ghcr.io/brsbsc/denova:${DENOVA_IMAGE_TAG:-latest}
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      DENOVA_USERNAME: ${DENOVA_USERNAME:?Set the initial login username}
      DENOVA_PASSWORD: ${DENOVA_PASSWORD:?Set the initial login password}
    volumes:
      - denova-data:/data
    restart: unless-stopped

volumes:
  denova-data:
